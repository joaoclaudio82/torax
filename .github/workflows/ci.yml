name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: thorax-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Create environment
        run: python -m venv .venv

      - name: Install dependencies
        run: .venv/bin/python -m pip install -r requirements.txt pytest

      - name: Verify dependency graph
        run: .venv/bin/python -m pip check

      - name: Compile Python
        run: .venv/bin/python -m compileall -q *.py scripts

      - name: Import hardened entrypoint
        run: .venv/bin/python -c "from entrypoint import base_app; assert base_app.version == '2.3.0'"

      - name: Run fast suite
        run: npm run test:fast

  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Create environment
        run: python -m venv .venv

      - name: Install dependencies
        run: .venv/bin/python -m pip install -r requirements.txt pytest

      - name: Verify dependency graph
        run: .venv/bin/python -m pip check

      - name: Cache model weights
        uses: actions/cache@v4
        with:
          path: |
            ~/.torchxrayvision
            ~/.cache/torch
          key: torchxrayvision-${{ runner.os }}-densenet121

      - name: Run smoke pipeline
        run: npm run test:full
        env:
          TORCHXRAYVISION_CACHE: ${{ github.workspace }}/.torchxrayvision

  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build runtime image
        run: docker build --tag thorax:ci .
